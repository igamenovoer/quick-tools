# InvokeAI Docker Environment Configuration
# Copy this file to .env and modify as needed

# Network Configuration
# The port InvokeAI will listen on
INVOKEAI_PORT=9090

# User ID
# The User ID to run the container as. 
# It works best if this matches your host user's UID (run `id -u` to find out).
CONTAINER_UID=1000

# Host Directories (Where data is stored on your machine)
# Root directory for InvokeAI data (outputs, database, configuration)
HOST_INVOKEAI_ROOT=~/invokeai

# HuggingFace Cache
# Where HuggingFace models are cached locally
HOST_HF_HOME=~/.cache/huggingface

# External Models
# Checkpoint models (.ckpt, .safetensors) to mount into the container
HOST_EXTERNAL_MODELS_DIR=~/invokeai/sd_models

# User Output Directory
# Where generated images are saved on the host
HOST_USER_OUTPUT_DIR=~/invokeai/outputs

# Helper Variables (Usually don't need to change these)
# HuggingFace Mirror (Optional, for regions with limited access)
# HF_ENDPOINT=https://hf-mirror.com
