# Normalize text files to LF in the repository and on checkout by default.
* text=auto eol=lf

# Linux/Unix scripts must always checkout with LF.
*.sh   text eol=lf
*.bash text eol=lf

# Windows scripts: store LF in repo, checkout CRLF on Windows working trees.
*.bat    text eol=crlf
*.cmd    text eol=crlf
*.ps1    text eol=crlf
*.psm1   text eol=crlf
*.psd1   text eol=crlf
*.ps1xml text eol=crlf
