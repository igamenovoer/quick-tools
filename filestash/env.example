# Copy to `.env` and adjust values as needed.
#
# Run Filestash as your current Linux user so mounted files are read/written with your UID/GID.
# Defaults (if unset): 1000:1000
PUID=1000
PGID=1000

# Host directory to browse in Filestash (mounted into the container at /data)
FILESTASH_DATA_DIR=/data2/huangzhe

# Ports exposed by the nginx reverse proxy
FILESTASH_HTTP_PORT=11088
FILESTASH_HTTPS_PORT=11089

# Optional: Filestash public host used to generate absolute URLs (share links / redirects).
# Leave empty to rely on incoming Host / X-Forwarded-* headers from nginx.
FILESTASH_APPLICATION_URL=
